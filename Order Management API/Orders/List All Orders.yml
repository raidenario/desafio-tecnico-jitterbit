info:
  name: List All Orders
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/order/list"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
