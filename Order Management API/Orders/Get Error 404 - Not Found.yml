info:
  name: Get Error 404 - Not Found
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/order/v-invalid-123"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
