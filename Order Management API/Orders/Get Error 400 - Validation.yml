info:
  name: Get Error 400 - Validation
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/order"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "numeroPedido": "v10089015vdb-02",
        "valorTotal": 10000,
        "items": []
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
